# niby ma dzialac


yolo mozna podkrecic bo narazie jest na tych srednich ( 50MB )


mozna pozmieniac w  `config.py`:

```python
detection_confidence = 0.3  
ball_confidence = 0.2       
device = "mps"               mps/cuda/cpu
```

preferable duzy kat nagrania 

# jak odpalac

trzeba dodac `input_video.mp4`
```bash
chmod +x run.sh   
./run.sh input_video.mp4 output_video.mp4
```


# la-machina
